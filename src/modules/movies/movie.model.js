import conn from '../../config/dbconfig';

const MovieModel = () => {
};

MovieModel.getAll = () => {
};

MovieModel.get = () => {
};

MovieModel.insert = () => {
};

MovieModel.update = () => {
};

MovieModel.delete = () => {
};

export default MovieModel;

